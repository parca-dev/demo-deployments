oauth2-proxy:
  ingress:
    hosts:
    - oauth2.parca.dev
    tls:
    - hosts:
      - oauth2.parca.dev
      secretName: oauth2.parca.dev
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
